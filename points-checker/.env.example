HOST=127.0.0.1
PORT=8891
ACCOUNTS_DB_PATH=../data/accounts.db
MAX_CONCURRENCY=auto
CHECK_TIMEOUT_MS=180000

# Optional browser login for the local dashboard.
DASHBOARD_USERNAME=
DASHBOARD_PASSWORD=

# Required only when credentials in accounts.db are encrypted.
ACCOUNTS_DB_KEY=
