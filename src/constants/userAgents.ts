// The Rewards app/platform user-agent is now derived per account (one coherent
// Android "Bing Sapphire" identity per phone) in src/browser/DeviceIdentity.ts.
// The old single hardcoded iOS iPad UA was shared by every account and mismatched
// the Android browser session — see buildAppUserAgent / deriveMobileDeviceIdentity.
export {}
